
package com.example.auction_service.domain.validation;


import java.math.BigDecimal;
import java.util.UUID;

public class BasePriceRule implements BidRule {
//  public void validate(AuctionAggregate agg, UUID playerId, UUID userId, BigDecimal amount){
//    BigDecimal base = agg.getBasePrice(playerId);
//    if (base != null && amount.compareTo(base) < 0)
//      throw new IllegalArgumentException("Below base price");
//  }
}
