
package com.example.auction_service.domain.validation;


import java.math.BigDecimal;
import java.util.UUID;

public interface BidRule {
//  void validate(AuctionAggregate agg, UUID playerId, UUID userId, BigDecimal amount);
}
