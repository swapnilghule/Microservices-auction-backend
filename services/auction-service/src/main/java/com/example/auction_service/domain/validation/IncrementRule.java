
package com.example.auction_service.domain.validation;


import java.math.BigDecimal;
import java.util.UUID;

public class IncrementRule implements BidRule {
//  private final BigDecimal step;
//  public IncrementRule(BigDecimal step){ this.step = step; }
//  public void validate(AuctionAggregate agg, UUID playerId, UUID userId, BigDecimal amount){
//    BigDecimal cur = agg.getCurrentAmount(playerId);
//    if (cur != null && amount.subtract(cur).remainder(step).compareTo(BigDecimal.ZERO) != 0)
//      throw new IllegalArgumentException("Must increase by step " + step);
//  }
}
